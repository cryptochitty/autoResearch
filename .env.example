# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional — controls which Claude model is used (defaults to sonnet for cost)
MODEL=claude-sonnet-4-6

# Optional — how many papers to fetch (default 10)
MAX_PAPERS=10

# Optional — output directory for saved reports
OUTPUT_DIR=outputs
